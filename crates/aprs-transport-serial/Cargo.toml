[package]
name = "aprs-transport-serial"
version = "1.7.0"
edition = "2021"
rust-version = "1.80"
license = "MIT"
description = "Serial reader helpers for libaprs-engine"
repository = "https://github.com/elodiejmirza/libaprs-engine"
readme = "../../README.md"
keywords = ["aprs", "serial", "packet", "tnc"]
categories = ["parser-implementations", "encoding"]

[dependencies]
libaprs-engine = { version = "1.7.0", path = "../libaprs-engine" }
