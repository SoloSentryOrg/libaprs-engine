[package]
name = "aprs-transport-mqtt"
version = "2.0.0-rc.2"
edition = "2021"
rust-version = "1.80"
license = "MIT"
description = "MQTT payload helpers for libaprs-engine"
repository = "https://github.com/SoloSentryOrg/libaprs-engine"
readme = "../../README.md"
keywords = ["aprs", "mqtt", "packet", "transport"]
categories = ["parser-implementations", "encoding"]

[dependencies]
libaprs-engine = { version = "2.0.0-rc.2", path = "../libaprs-engine" }
