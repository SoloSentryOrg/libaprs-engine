[package]
name = "aprs-transport-file-watch"
version = "0.2.0"
edition = "2021"
rust-version = "1.80"
license = "MIT"
description = "Append-only file watch helpers for libaprs-engine"
repository = "https://github.com/elodiejmirza/libaprs-engine"
readme = "../../README.md"
keywords = ["aprs", "file", "watch", "packet"]
categories = ["parser-implementations", "encoding"]

[dependencies]
libaprs-engine = { version = "0.2.0", path = "../libaprs-engine" }
