[package]
name = "aprs-transport-http"
version = "0.3.0"
edition = "2021"
rust-version = "1.80"
license = "MIT"
description = "HTTP body ingestion helpers for libaprs-engine"
repository = "https://github.com/elodiejmirza/libaprs-engine"
readme = "../../README.md"
keywords = ["aprs", "http", "packet", "transport"]
categories = ["parser-implementations", "encoding"]

[dependencies]
libaprs-engine = { version = "0.3.0", path = "../libaprs-engine" }
